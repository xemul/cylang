<+ < $ Args 2

! f <~ Args.1
! c (: << f "c"

! pop8 {
	! o ( ) ! n 0
	~+ -( i { +) o _ <+ = 7 !% n + _ 1 }
	<! o
}

! l 0
~+ c.0 {
	! s1 -> pop8 [ i c ]
	! s2 -> pop8 [ i c ]

	! v ""
	~( s1 { ! v + v %% "\(_:%02x) " }
	~( s2 { ! v + v %% " \(_:%02x)" }

	! s ""
	~( + s1 s2 { ! s + s ? & <= _ 126 >= _ 33 { <! %% "\(_:%c)" } { <! "." } }

	` %% "\(l:%08x)  \(v)  |\(s)|"

	! l + l 16
}
