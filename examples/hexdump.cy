<+ ;- < $ Args 2

! f <~ Args.1
! c (: << f "c"

! pop8 { ! o ( ) ! n 0 ~+ -( i { +) o _ <+ ;- = 7 !% n + _ 1 } <! o }
! chr { <! ? & <= c 126 >= c 33 { <! %% "\(c:%c)" } { <! "." } }

! l 0
~+ c.0 {
	! s1 -> pop8 [ i c ] ! s2 -> pop8 [ i c ]

	! v "" ! s ""
	~( s1 { ! v + v %% "\(_:%02x) "
		! s + s -> chr [ c _ ]
	}
	~( s2 { ! v + v %% " \(_:%02x)"
		! s + s -> chr [ c _ ]
	}

	` %% "\(l:%08x)  \(v)  |\(s)|"
	!% l + _ 16
}
